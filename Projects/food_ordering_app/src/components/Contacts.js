const Contacts = () => {
    return (
        <div>
            <h1>Contacts Page</h1>
            <h2>You reached by routing to "/contacts"!!</h2>
        </div>
        
    );
};

export default Contacts;